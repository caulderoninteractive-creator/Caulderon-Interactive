(async()=>{const host=document.querySelector('#livejobs');try{const r=await fetch('meritniti/jobs/feed.json',{cache:'no-store'});if(!r.ok)throw Error();const rows=await r.json();const today=new Intl.DateTimeFormat('en-CA',{timeZone:'Asia/Kolkata',year:'numeric',month:'2-digit',day:'2-digit'}).format(new Date());const selected=rows.filter(j=>['open','closing_soon','upcoming'].includes(j.application_status)&&j.deadline>=today).sort((a,b)=>a.deadline.localeCompare(b.deadline)).slice(0,3);host.replaceChildren();if(!selected.length){host.textContent='No confirmed active openings to show. Check the jobs board for the next update.';return}for(const j of selected){let card=document.createElement('article');card.className='panel';let badge=document.createElement('span');badge.className='pill';badge.textContent=j.application_status.replaceAll('_',' ');let h=document.createElement('h3'),a=document.createElement('a');a.textContent=j.title;const u=new URL(j.url,location.href);a.href=u.pathname;h.append(a);let p=document.createElement('p');p.className='muted';p.textContent=(j.organisation||'')+' · Last date: '+j.deadline;card.append(badge,h,p);host.append(card)}}catch(e){host.textContent='The updates feed could not load. ';let a=document.createElement('a');a.href='meritniti/jobs/';a.textContent='Open the jobs board →';host.append(a)}})();
