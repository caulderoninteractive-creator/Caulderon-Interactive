CAULDERON INTERACTIVE — UI POLISH PASS, ROUND 2
15 September 2026

RE-APPLIED STRUCTURAL FIXES
The project file store had reverted to its unfixed state between sessions
(index.html was the Devlog page again, netprepquiz-*/styles.css/site-
enhancements.css/README.txt were back). Re-applied the same fixes as the
last pass: devlog now lives in devlog/, the real homepage is back at
index.html, and the orphaned legacy files are removed again.

Good news: site.css, meritniti.html and script.js had NOT reverted -- they
already carried forward last round's polish (the --ur token, nav/footer
gradient bar, hero glow, all-6-titles cross-promo grids) plus new
Government Jobs wiring that was added independently. Those were kept as-is
and only extended, not rebuilt.

REAL STORE BADGES
Swapped the generic placeholder icon+text badges for the actual Google
Play and App Store artwork you supplied (assets/google-play.png,
assets/app-store.png). Live vs. coming-soon is now shown by dimming the
real badge (grayscale + reduced brightness/opacity) and adding a small
"COMING SOON" ribbon in the corner, rather than by swapping in different
wording baked into the image, so Tilt Maze / Block Chakra / Number Run's
App Store badge can't be mistaken for a live listing.
  - Number Run: live Google Play badge + dimmed, ribboned App Store badge.
  - Tilt Maze, Block Chakra: dimmed, ribboned Google Play badge.
  - MeritNiti and the two RPGs: no badge, unchanged -- no store listing
    exists yet, so nothing was added there.

MERITNITI GOVERNMENT JOBS & EXAM UPDATES -- NOW LIVE IN THE PACKAGE
Added the three files you provided for https://caulderoninteractive.com/meritniti/jobs/:
  - meritniti/jobs/index.html -- kept your structure and content, added a
    favicon link, theme-color, valid &-escaped title/heading, an
    Open Graph block matching the rest of the site's pattern, and a
    Support link in the footer for consistency with every other page.
  - meritniti/jobs/feed.json and meritniti/jobs/sitemap.xml -- placed
    exactly as supplied (live generated data, not something for me to edit).
  - meritniti.html and script.js already fetch/link this correctly
    (basePath '/meritniti/jobs/'), so no wiring changes were needed there.

ONE GAP I DIDN'T FILL IN
feed.json and the jobs index page both link to
meritniti/jobs/india-post-gds-2026/ (the individual exam detail page),
but that page's content wasn't supplied. Per DEPLOY_README.txt this is
generated by the separate GovJobs scanner tool, and I didn't have
verified source material for that specific listing (vacancy count,
event history, etc.), so I left it out rather than invent one. Run the
scanner (or drop in the generated page) and that link resolves.

STILL TRUE FROM LAST ROUND
This package has no image binaries except the two store badges you just
uploaded. icon.png, favicon.png, and every title's icon still need to
come from you -- every <img> reference is untouched and will resolve as
soon as those files sit next to the HTML.
